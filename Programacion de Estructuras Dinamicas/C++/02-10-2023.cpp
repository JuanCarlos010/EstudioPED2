#include <iostream>
using namespace std;

//en un listado circular, para hacer la equivalencia de apuntar a un null ptr se hace que el ultimo nodo apunte al primer valor (asi dijeron xd)
struct Nodo
{
    int elemento;
    struct Nodo *siguiente;
};

struct Nodo *lista = nullptr

int main(){
    return 0;
}
